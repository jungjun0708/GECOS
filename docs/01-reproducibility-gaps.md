# GECOS 재현 가능성 차이 및 처리 방침

## 1. 문서 목적

이 문서는 출판 논문, 원저자 공개 코드, 공개 데이터만으로 확정할 수 없는 사항을
추적한다. 불확실성을 구현 세부사항 속에 숨기지 않고 각 항목에 고유한 gap ID와
처리 방침을 부여한다.

상세 실험 규칙은 [핵심 재현 범위와 실험 계약](00-reproduction-scope.md)을 따른다.

상태는 다음 세 가지로 관리한다.

- `결정 완료`: 이 저장소의 구현 방침을 명시함
- `검증 필요`: 구현 또는 실험 결과로 확인해야 함
- `외부 정보 필요`: 공개 자료만으로 정확한 저자 설정을 알 수 없음

## 2. 공개 코드와 UPC

### GAP-UPC-01: 공개 클러스터링 코드는 논문의 UPC가 아님

- 상태: `결정 완료`
- 근거: 공개 `main.py`는 완성된 correlation matrix를 입력받아 변수를 초기
  round-robin 그룹에 배치한 뒤 평균 상관을 높이는 방향으로 이동한다.
- 논문: 셀의 평일 peak hour로 24개 그룹을 만든 후 그룹 profile의 PCC를 사용한다.
- 영향: 공개 코드를 그대로 실행해도 논문의 Fig. 4 및 Fig. 5를 재현할 수 없다.
- 방침: 논문 Algorithm 1을 기준으로 UPC를 새로 구현하고 공개 코드는 참고용으로만
  보존한다.

### GAP-UPC-02: `correlation_matrix_0721.csv`가 공개되지 않음

- 상태: `결정 완료`
- 근거: README와 `main.py`가 파일을 참조하지만 저장소에는 존재하지 않는다.
- 영향: 공개 clustering 예제를 별도 데이터 없이 실행할 수 없다.
- 방침: 이 파일을 핵심 재현의 입력으로 사용하지 않는다. 향후 생성하더라도
  파생 데이터로 취급하여 Git에서 제외한다.

### GAP-UPC-03: 작은 peak-hour 그룹 처리 방식이 불명확함

- 상태: `결정 완료`
- 논문: 크기가 `theta=10`보다 큰 그룹에 대해서만 PCC를 계산한다고 설명하지만,
  작은 그룹을 최종 cluster에 배정하는 세부 규칙은 분명하지 않다.
- 영향: 최종 cluster membership과 크기가 달라질 수 있다.
- 방침: 작은 그룹은 seed 후보에서는 제외하되 길이 24 profile을 계산하여 최종
  cluster 배정에는 포함한다.

### GAP-UPC-04: 그룹 PCC에 사용하는 profile 정의가 불명확함

- 상태: `결정 완료`
- 논문: 그룹 traffic을 평균한다고 설명하지만 PCC 벡터의 정확한 시간축 구성을
  완전히 공개하지 않는다.
- 영향: seed 그룹과 최종 병합 결과가 바뀔 수 있다.
- 방침: 셀과 선택 기간의 평일을 평균한 길이 24의 시간별 traffic profile을 사용한다.

### GAP-UPC-05: tie-breaking 규칙이 없음

- 상태: `결정 완료`
- 영향: 같은 peak 빈도 또는 PCC를 가진 항목에서 실행마다 결과가 달라질 수 있다.
- 방침: peak hour는 가장 이른 hour, cluster는 가장 작은 ID를 선택한다.

## 3. 공개 코드와 RCTL

### GAP-RCTL-01: `Add`와 `Concatenate` 불일치

- 상태: `검증 필요`
- 공개 코드: TCN 출력과 1x1 convolution shortcut을 `Add`로 결합한다.
- 논문 Fig. 2: 해당 위치를 `Concatenate`로 표시한다.
- 영향: LSTM 입력 channel 수와 전체 parameter 수가 달라진다.
- 방침: 논문형을 주 구현으로 사용하고 공개 코드형을 `public_reference`로 분리해
  shape와 parameter 수를 비교한다.

### GAP-RCTL-02: convolution kernel size 불일치

- 상태: `검증 필요`
- 공개 코드: kernel size 3
- 논문 부록 Fig. 12: filter size 4
- 영향: receptive field와 parameter 수가 달라진다.
- 방침: 논문형은 4, 공개 코드형은 3을 사용한다.

### GAP-RCTL-03: dilation 규칙 불일치

- 상태: `검증 필요`
- 공개 코드: 첫 층 1, 이후 `2*i`이므로 `1, 2, 4, 6, 8, 10`
- 논문 Table I: `2^i`
- 영향: 깊은 층의 receptive field가 크게 달라진다.
- 방침: 논문형은 `1, 2, 4, 8, 16, 32`를 사용한다.

### GAP-RCTL-04: RCC 연결 위치의 정확한 연산 순서가 불명확함

- 상태: `검증 필요`
- 근거: 논문 그림과 공개 코드에서 RCC-1/RCC-2 연결 및 중간 1x1 convolution의
  적용 위치가 완전히 일치하지 않는다.
- 영향: feature shape, gradient 경로와 parameter 수가 달라진다.
- 방침: 논문 그림을 따라 tensor shape 표를 먼저 작성하고, 블록 단위 테스트를
  통과한 구현만 학습에 사용한다.

### GAP-RCTL-05: 논문 parameter count 173,633의 재구성 여부

- 상태: `검증 필요`
- 근거: 논문 Table III는 RCTL parameter 수를 173,633으로 보고한다.
- 영향: 구조가 같아 보이더라도 실제 구현이 다를 수 있다.
- 방침: `model.count_params()`를 회귀 테스트로 추가한다. 일치하지 않으면 숫자에
  맞추기 위한 임의 변경을 하지 않고 각 구현의 실제 수를 결과에 기록한다.

### GAP-RCTL-06: early stopping 상세값이 없음

- 상태: `결정 완료`
- 논문: 평균 약 20 epoch를 사용했다고 설명하지만 patience와 최대 epoch를 공개하지 않는다.
- 공개 코드: 최대 1,000 epoch와 checkpoint만 있고 EarlyStopping callback은 없다.
- 방침: 최대 1,000 epoch, patience 5, validation MAE 감시, 최적 가중치 복원을
  기본값으로 사용하고 모두 config에 노출한다.

## 4. 데이터와 표본 생성

### GAP-DATA-01: 공란과 완전히 없는 셀-시점의 의미

- 상태: `결정 완료`
- 근거: 원본에는 활동 열 공란과 셀-시점 조합 자체의 누락이 모두 존재한다.
- 영향: traffic 합계, peak hour, MAPE가 달라질 수 있다.
- 방침: 두 경우 모두 traffic 0으로 처리한다. 셀-시점 행 자체가 없으면
  `missing_mask`, 행은 있지만 모든 Internet 값이 공란이면 `internet_null_mask`로
  기록하고 활동 열별 공란 행 수도 manifest에 보존한다.

### GAP-DATA-02: scaling 대상과 적합 기간이 불명확함

- 상태: `결정 완료`
- 논문: UPC 입력이 min-max scaled라고 설명하지만 셀별/전체 기준과 모델 입력
  scaling 여부를 공개하지 않는다.
- 영향: 그룹 평균 profile과 모델 최적화가 달라질 수 있다.
- 방침: UPC에는 셀별 min-max scaling을 적용한다. 주 결과는 Train 기간에만
  scaler를 적합한다. RCTL의 최초 paper-oriented 실행은 raw traffic을 사용하고,
  추가 scaling은 별도 실험으로 취급한다.

### GAP-DATA-03: 길이 `q`와 예측 target 정렬이 불명확함

- 상태: `결정 완료`
- 논문: 길이 `q`의 overlapping sequence 수를 `4320-(q-1)`로 설명하지만, 각
  sequence에서 어느 값을 target으로 삼는지 명시하지 않는다.
- 영향: 현재 값을 복원하는 문제와 다음 시점을 예측하는 문제가 혼동될 수 있다.
- 방침: 과거 8개 값을 입력으로 사용해 다음 값을 예측하며 셀당 4,312개 표본을 만든다.

### GAP-DATA-04: validation/test 경계가 없음

- 상태: `결정 완료`
- 논문: 앞 20일은 학습, 뒤 10일은 validation과 testing이라고만 설명한다.
- 영향: early stopping과 최종 성능에 같은 데이터가 사용될 수 있다.
- 방침: 주 결과는 20일 Train, 5일 Validation, 5일 Test로 분리한다. 뒤 10일 전체
  지표는 논문 비교용 보조값으로만 표시한다.

### GAP-DATA-05: 중앙 900셀 ID가 공개되지 않음

- 상태: `외부 정보 필요`
- 논문: 중앙 30x30 영역을 그림으로 제시하지만 정확한 ID 목록과 경계가 없다.
- 영향: 논문과 다른 교통 분포를 학습할 수 있다.
- 방침: Milano Grid centroid 기준 기하학적 중앙 30x30을 사용하고 결과에
  `approximate`를 표시한다. 생성된 ID 목록과 지도 이미지를 보존한다.

## 5. 평가와 보고

### GAP-METRIC-01: MAPE 단위가 일관되지 않음

- 상태: `결정 완료`
- 논문 식: 100을 곱한 백분율
- 논문 표: `0.1000`처럼 비율로 보이는 값
- 영향: 같은 결과가 0.1 또는 10%로 다르게 읽힐 수 있다.
- 방침: MAPE ratio와 MAPE percent를 별도 열로 동시에 출력한다.

### GAP-METRIC-02: MAPE의 0 target 처리 방식이 없음

- 상태: `결정 완료`
- 영향: 0으로 나누거나 작은 값이 전체 평균을 지배할 수 있다.
- 방침: `y > 0` 표본만으로 MAPE를 계산하고 제외된 표본 수와 비율을 보고한다.
  WAPE를 보조 지표로 함께 사용한다.

### GAP-METRIC-03: 평균 방식이 불명확함

- 상태: `결정 완료`
- 영향: 전체 표본 micro average와 셀별 macro average가 서로 다른 결론을 낼 수 있다.
- 방침: 논문 비교용 micro average와 공간적 편차 확인용 cell-macro average를 모두
  출력하고 열 이름에 집계 방식을 포함한다.

### GAP-METRIC-04: 99% 신뢰구간의 계산법이 없음

- 상태: `외부 정보 필요`
- 근거: 논문은 99% 신뢰구간을 제시하지만 반복 횟수, seed, 표본 단위와 계산법을
  공개하지 않는다.
- 방침: seed 3개의 개별 결과, 평균과 표준편차를 보고한다. 동일한 방법이라고
  확인되기 전에는 논문의 99% 신뢰구간을 재현했다고 표현하지 않는다.

### GAP-METRIC-05: 10,000셀 UPC 향상률 설명과 표가 불일치함

- 상태: `외부 정보 필요`
- 논문 Table IV: MAPE가 UPC 미적용 0.6782, 적용 0.1186으로 표시됨
- 논문 본문: 약 42% 향상이라고 설명함
- 영향: 표의 값을 기준으로 계산한 상대 감소율과 본문 주장이 일치하지 않는다.
- 방침: 표 수치와 본문 주장을 각각 인용하되 어느 쪽에도 맞추지 않는다. 우리
  결과의 향상률은 원시 결과로 다시 계산한다.

### GAP-METRIC-06: 예시 셀 ID가 본문과 그림 설명에서 다름

- 상태: `외부 정보 필요`
- 근거: Fig. 9 설명은 cell 5259, 인접 본문은 cell 5229를 언급한다.
- 영향: 예시 시계열의 정확한 재현이 어려움
- 방침: 핵심 성능 검증에는 사용하지 않는다. 예시 그림을 만들면 선택한 cell ID를
  config와 파일명에 명시한다.

## 6. 코드와 라이선스

### GAP-REPO-01: 공개 저장소에 소프트웨어 라이선스가 없음

- 상태: `외부 정보 필요`
- 영향: 공개 코드의 재배포 및 파생 코드 라이선스를 임의로 단정할 수 없다.
- 방침: 원저자 이력을 유지하고 출처를 표시한다. 원저자 저장소 전체를 포괄하는
  새 라이선스를 임의로 추가하지 않는다.

### GAP-REPO-02: 실행 환경이 고정되지 않음

- 상태: `검증 필요`
- 근거: requirements, Python/TensorFlow/Keras/CUDA 버전이 공개되지 않았다.
- 영향: 최신 Keras에서 저장 형식과 일부 API 동작이 달라질 수 있다.
- 방침: 첫 동작 구현에서 실제 Colab runtime을 기록하고 호환되는 버전을 lock한다.
  모든 결과에 환경 정보를 저장한다.

### GAP-REPO-03: 데이터와 논문의 배포 조건

- 상태: `결정 완료`
- 데이터: Telecom Italia 데이터는 ODbL 1.0 조건과 출처 표기가 필요하다.
- 논문: 로컬 참고 사본을 저장소에 포함하지 않는다.
- 방침: 원시 데이터, 파생 대용량 데이터와 논문 PDF를 Git에서 제외한다. 문서에는
  원출처 DOI와 라이선스를 기록한다.

## 7. gap 관리 규칙

1. 구현 선택이 바뀌면 관련 gap의 기존 내용을 삭제하지 않고 변경 사유를 기록한다.
2. 논문과 다른 설정은 config 이름과 결과 표에 표시한다.
3. 외부 정보가 확인되면 출처 URL, 확인 날짜와 commit을 남긴다.
4. gap을 해결하기 위해 결과를 본 뒤 parameter를 임의 조정하지 않는다.
5. 실험 결과 문서는 관련 gap ID를 참조한다.
6. 새로운 불확실성을 발견하면 구현 전에 먼저 이 문서에 등록한다.

이 문서의 목적은 모든 불확실성을 제거하는 것이 아니라, 재현 결과의 한계를
다른 사람이 정확히 판단할 수 있게 만드는 것이다.
